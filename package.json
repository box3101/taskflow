{
  "name": "taskflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd frontend && NODE_ENV=development npm install && cd ../backend && NODE_ENV=development npm install",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build": "npm run build:frontend && npm run build:backend",
    "db:generate": "cd backend && npx prisma generate",
    "db:migrate": "cd backend && npx prisma migrate deploy",
    "db:seed": "cd backend && npx ts-node-dev src/seed.ts",
    "start": "cd backend && node dist/src/app.js",
    "postinstall": "npm run install:all && npm run db:generate && npm run db:migrate && npm run build"
  },
  "dependencies": {
    "@leechanyong/ispark-ui": "github:box3101/ispark-ui#9b278e6e44bad955ab316f79258e10c726881a5b"
  }
}
